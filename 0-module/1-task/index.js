/**
 * sum
 * @param {number} m
 * @param {number} n
 * @returns {number}
 */
function sum(number1, number2) {
  let summa = number1+number2;
  return summa;  
}
let summa1 = sum(1,1);
console.log ("sum(1,1)=",summa1);
let summa2 = sum(2,5);
console.log ("sum(2,5)=",summa2);